# Master .env (project root) — used for:
# - CLI: Give feedback / Read feedback
# - Agent register: when run from agents/<name>, this file is loaded so the same
#   PRIVATE_KEY and PINATA_JWT are used. You can then fund the agent wallet from here.

# Required for registration and giving feedback
PRIVATE_KEY=0x...

# RPC URL (optional; agents can override per chain)
RPC_URL=https://sepolia-rollup.arbitrum.io/rpc

# Pinata JWT for IPFS (used by register and by CLI)
PINATA_JWT=your_pinata_jwt_here

# OpenAI (only needed inside each agent's .env for running the agent server)
# OPENAI_API_KEY=...
